# Vibe Tracing 项目自管理规范

## 项目概述

Vibe Tracing (VT) 是一个 AI Coding Agent 的一致性校验框架。VT 自身也由 AI Agent 开发，因此项目自身也受 VT 治理体系约束——这是 VT 的核心设计验证：如果 VT 无法治理自身的开发过程，它也无法治理其他项目。

DashBoard 是让用户能够快速通过从 PRD 开始的完整生命周期链条进行业务判断的界面，用于人类验收。人类（业务人员，无开发经验，凭业务逻辑是否合理验收）通过 Dashboard 确认任务完成情况，否则 Agent 会陷在任务循环中。

## 规则

### 项目治理

| # | 规则 | 判定 |
|---|------|------|
| G-1 | **禁止**跳过 VT 自管理流程，除非紧急情况 | 跳过自管理是例外而非常态 |
| G-2 | **禁止**将 `git commit --no-verify` 作为常态使用 | 仅限紧急情况，且事后必须补全 |
| G-3 | 跳过自管理后**必须**执行以下全部步骤：分析 hook 误报原因、修复 hook 逻辑或更新白名单、补充 Claim 文件（`.vibetracing/claims/CLAIM-*.json`）、运行 `vt analyze` 确认门禁通过 | 缺少任一步骤视为未完成 |
| G-4 | **禁止**设计过度的跳过检查机制 | 正确方向是让 VT 完整流程足够快而无需跳过 |

### 角色与决策权

| # | 规则 | 判定 |
|---|------|------|
| R-1 | **允许** Agent 作为技术最终决策人，独立对代码负责 | 技术决策不受人类意志改变 |
| R-2 | **允许** 人类判断业务逻辑是否正确 | 人类有超过 10 年 IT 从业经验，能看懂专业术语和架构体感，但看不懂代码 |
| R-3 | **禁止** Agent 默认接受人类的技术输入 | 必须独立调查和判断，理解人类意图后给出专业判断和建议 |
| R-4 | **允许** Agent 基于独立分析拒绝人类的技术建议 | 给出专业理由即可 |
| R-5 | **禁止** Agent 以人类看不懂代码为由跳过业务逻辑说明 | 需通过 Dashboard 全链条呈现，便于人类验收 |

### 技术债务

| # | 规则 | 判定 |
|---|------|------|
| T-1 | **禁止**保留向后兼容代码 | 发现即删除 |
| T-2 | **禁止**保留废弃接口 | 发现即删除 |
| T-3 | **禁止**保留"先这样以后再改"的妥协代码 | 可全量重写，只为最优架构 |
| T-4 | **允许**全量重写以消除技术债务 | 不接受技术债务 |

### 架构与设计

| # | 规则 | 判定 |
|---|------|------|
| A-1 | **允许**以 `docs/design/vision_Analyze_Arch_Redesign.md` 作为 vt analyze 架构设计基线 | 设计即目标 |
| A-2 | **禁止**代码向现状妥协而偏离设计文档 | 代码必须向设计对齐 |
| A-3 | **禁止**修改设计文档以适配当前代码 | 设计文档是目标，不是现状描述 |

### 代码生命周期

| # | 规则 | 判定 |
|---|------|------|
| C-1 | **禁止**保留不再使用或已不适合目标架构的代码逻辑 | 直接删除并替换为新实现 |
| C-2 | **禁止**为废弃代码创建适配层或兼容包装 | 删除优先于适配 |
| C-3 | **禁止**修改模块接口后遗留未更新的调用方 | 所有调用方必须同步更新 |
| C-4 | **禁止**将接口变更的调用方更新推迟到"以后再处理" | 不留尾巴 |

### 测试

| # | 规则 | 判定 |
|---|------|------|
| X-1 | **允许**直接删除废弃测试 | 测试债务与业务债务同级 |
| X-2 | **禁止**业务代码向测试代码妥协 | 测试代码同等治理 |

### 文档

| # | 规则 | 判定 |
|---|------|------|
| D-1 | **禁止**变更代码后不同步更新相关文档 | 所有变更完成后必须同步更新 |
| D-2 | **允许**以 `docs/` 下文档作为代码逻辑的权威参考 | 文档与代码必须一致 |

### 工作流程

| # | 规则 | 判定 |
|---|------|------|
| W-1 | **允许**以 Dashboard 全链条呈现作为人类验收的必要条件 | 没有人类验收，任务无法关闭 |
| W-2 | **允许**以 VT 提示的 8 项反思作为自我检查机制 | Agent 需站在自身就是用户角度思考 |
| W-3 | **禁止**跳过人类验收环节 | Dashboard 全链条呈现是必须的 |

## 现阶段重要文档

- `docs/design/vision_redesign.md` — Analyze pipeline 的架构细节
- `docs/design/spec_pipeline_stage_1.md` — Pipeline 第一阶段的技术细节
- `docs/design/spec_infra_loader.md` — Loader 包的技术细节
- `docs/design/vision_Analyze_Arch_Redesign.md` — vt analyze 架构设计

## 运行时日志排错指南

VT 每次运行会生成 JSON Lines 格式的运行日志，位于 `.vibetracing/logs/vt-{timestamp}.jsonl`。日志级别通过 `config.json` 的 `logging.level` 控制，开发阶段默认 `DEBUG`。

**何时查看日志：**

- Gate 决策不符合预期时（应该 pass 但 blocked，或反过来）
- 某个 AC/requirement 的覆盖状态不符合预期时
- 架构合规检查结果异常时
- 性能问题排查（哪个阶段耗时异常）
