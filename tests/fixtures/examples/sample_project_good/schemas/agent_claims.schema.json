{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://vibe-tracing/schemas/agent_claims.schema.json",
  "title": "AgentClaims",
  "description": "Schema for individual CLAIM-*.json claim objects",
  "type": "array",
  "items": {
    "type": "object",
    "required": [
      "claim_id",
      "related_task",
      "timestamp"
    ],
    "additionalProperties": true,
    "properties": {
      "claim_id": {
        "type": "string",
        "pattern": "^CLAIM-VT-\\d+$",
        "description": "Unique claim identifier."
      },
      "related_task": {
        "type": "string",
        "pattern": "^TASK-VT-\\d+$",
        "description": "Task ID this claim is associated with."
      },
      "timestamp": {
        "type": "string",
        "description": "ISO-8601 timestamp when the claim was made."
      },
      "code_refs": {
        "type": "array",
        "description": "Optional references to source code locations.",
        "items": {
          "type": "string"
        }
      },
      "test_refs": {
        "type": "array",
        "description": "Optional references to test file locations.",
        "items": {
          "type": "string"
        }
      },
      "notes": {
        "type": "string",
        "description": "Optional free-form notes from the agent."
      }
    }
  }
}
